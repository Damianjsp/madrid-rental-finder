# =============================================================================
# SECRET TEMPLATE — DO NOT COMMIT REAL VALUES
# =============================================================================
#
# This file is a TEMPLATE. It must be created manually on the cluster
# or managed via a secrets manager (e.g. ExternalSecrets, SOPS, Vault).
#
# HOW TO CREATE THE SECRET MANUALLY:
#
#   kubectl create secret generic mrf-db-credentials \
#     --namespace madrid-rental-finder \
#     --from-literal=DATABASE_URL="postgresql+psycopg://<USER>:<PASS>@postgresql.postgresql.svc.cluster.local:5432/madrid_rental_finder"
#
# OR apply a sealed/encrypted version of this file if using SOPS or SealedSecrets.
#
# The DB credentials live in namespace 'postgresql' as 'postgresql-credentials'.
# Copy the relevant keys into this namespace.
#
# NEVER push real credentials to git.
# =============================================================================
#
# apiVersion: v1
# kind: Secret
# metadata:
#   name: mrf-db-credentials
#   namespace: madrid-rental-finder
# type: Opaque
# stringData:
#   DATABASE_URL: "postgresql+psycopg://<DB_USER>:<DB_PASSWORD>@postgresql.postgresql.svc.cluster.local:5432/madrid_rental_finder"
